import { useEffect, useState } from 'react';
import Layout from '../components/Layout';
import { apiFetch } from '../api/client';

const STATUS_LABEL = { active: 'Aktif', deleted: 'Silinmiş', unreachable: 'Erişilemiyor' };
const STATUS_STYLE = { active: 'bg-emerald-50 text-emerald-700', deleted: 'bg-red-50 text-red-600', unreachable: 'bg-amber-50 text-amber-700' };

function VideoMetrics({ video }) {
  if (video.views === null || video.views === undefined) return <p className="mt-1 text-xs text-gray-400">Henüz veri yok — "Apify ile güncelle"ye basın</p>;
  const likesText = video.likes === null || video.likes === undefined ? 'Gizli' : Number(video.likes).toLocaleString('tr-TR');
  return <p className="mt-1 text-xs text-gray-600">İzlenme: <strong>{Number(video.views).toLocaleString('tr-TR')}</strong> · Beğeni: <strong>{likesText}</strong> · Yorum: <strong>{Number(video.comments ?? 0).toLocaleString('tr-TR')}</strong> · Tahmini kazanç: <strong className="text-emerald-700">{Number(video.earning ?? 0).toLocaleString('tr-TR')} TL</strong></p>;
}

export default function Videos() { const [videos, setVideos] = useState([]); const [users, setUsers] = useState([]); const [projects, setProjects] = useState([]); const [form, setForm] = useState({ projectId: '', ownerUserId: '', platform: 'instagram', url: '' }); const [error, setError] = useState(''); async function load() { const [videoData, userData, projectData] = await Promise.all([apiFetch('/videos'), apiFetch('/users'), apiFetch('/projects')]); setVideos(videoData.videos); setUsers(userData.filter((user) => user.role === 'influencer' || user.role === 'rapmedia')); setProjects(projectData.projects); } useEffect(() => { load().catch((err) => setError(err.message)); }, []); async function submit(event) { event.preventDefault(); try { await apiFetch('/videos', { method: 'POST', body: JSON.stringify(form) }); setForm({ projectId: '', ownerUserId: '', platform: 'instagram', url: '' }); await load(); } catch (err) { setError(err.message); } } async function refresh(id) { try { await apiFetch(`/videos/${id}/refresh`, { method: 'POST' }); await load(); } catch (err) { setError(err.message); } } return <Layout title="Videolar"><div className="max-w-6xl mx-auto"><h1 className="text-3xl font-semibold">Videolar</h1><p className="mt-2 text-sm text-gray-500">Instagram ve TikTok video kayıtları. İzlenmeler yalnızca Apify gerçek sonucu geldikten sonra yazılır.</p><form onSubmit={submit} className="mt-6 grid gap-3 rounded-xl border border-gray-200 bg-gray-50 p-4 sm:grid-cols-2 lg:grid-cols-4"><select required value={form.projectId} onChange={(event) => setForm({ ...form, projectId: event.target.value })} className="rounded-lg border border-gray-200 bg-white px-3 py-2.5 text-sm"><option value="">Proje</option>{projects.map((project) => <option key={project.id} value={project.id}>{project.name}</option>)}</select><select required value={form.ownerUserId} onChange={(event) => setForm({ ...form, ownerUserId: event.target.value })} className="rounded-lg border border-gray-200 bg-white px-3 py-2.5 text-sm"><option value="">Video sahibi</option>{users.map((user) => <option key={user.id} value={user.id}>{user.display_name || user.username}</option>)}</select><select value={form.platform} onChange={(event) => setForm({ ...form, platform: event.target.value })} className="rounded-lg border border-gray-200 bg-white px-3 py-2.5 text-sm"><option value="instagram">Instagram</option><option value="tiktok">TikTok</option></select><input required type="url" placeholder="Video URL" value={form.url} onChange={(event) => setForm({ ...form, url: event.target.value })} className="rounded-lg border border-gray-200 bg-white px-3 py-2.5 text-sm" /><button className="rounded-lg bg-gray-900 px-4 py-2.5 text-sm font-medium text-white sm:col-span-2 lg:col-span-4">Video ekle</button></form>{error && <p className="mt-4 text-sm text-red-600">{error}</p>}<div className="mt-6 space-y-2">{videos.map((video) => <div key={video.id} className={`flex flex-col gap-3 rounded-xl border border-gray-200 p-4 sm:flex-row sm:items-center sm:justify-between transition ${video.status !== 'active' ? 'opacity-55 grayscale-[35%]' : ''}`}><div className="min-w-0"><p className="flex flex-wrap items-center gap-2 font-medium"><span className="capitalize">{video.platform}</span><span className="text-gray-300">·</span><span>{video.project_name || 'Proje yok'}</span><span className={`rounded-full px-2 py-0.5 text-xs font-medium ${STATUS_STYLE[video.status]}`}>{STATUS_LABEL[video.status]}</span></p><a href={video.url} target="_blank" rel="noreferrer" className="block truncate text-sm text-gray-500 underline">{video.url}</a><p className="mt-1 text-xs text-gray-400">{video.owner_name || 'Sahip yok'}</p><VideoMetrics video={video} /></div><button onClick={() => refresh(video.id)} className="shrink-0 rounded-lg border border-gray-200 px-3 py-2 text-sm hover:bg-gray-50">Apify ile güncelle</button></div>)}</div></div></Layout>; }
