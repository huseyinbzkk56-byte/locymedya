import { useState } from 'react';
import Layout from '../components/Layout';
import { apiFetch } from '../api/client';
export default function MediaLinks() { const [form, setForm] = useState({ platform: 'instagram', url: '' }); const [message, setMessage] = useState(''); async function submit(event) { event.preventDefault(); try { await apiFetch('/links', { method: 'POST', body: JSON.stringify(form) }); setForm({ platform: 'instagram', url: '' }); setMessage('Link admin listesine eklendi.'); } catch (err) { setMessage(err.message); } } return <Layout title="Link Ekle"><div className="max-w-xl mx-auto"><h1 className="text-3xl font-semibold">Link Ekle</h1><p className="mt-2 text-sm text-gray-500">Gönderi linkini PR arşivine iletin.</p><form onSubmit={submit} className="mt-6 space-y-3 rounded-xl border border-gray-200 p-4"><select value={form.platform} onChange={(event) => setForm({ ...form, platform: event.target.value })} className="w-full rounded-lg border border-gray-200 px-3 py-2.5 text-sm"><option value="instagram">Instagram</option><option value="tiktok">TikTok</option><option value="x">X</option></select><input required type="url" placeholder="https://..." value={form.url} onChange={(event) => setForm({ ...form, url: event.target.value })} className="w-full rounded-lg border border-gray-200 px-3 py-2.5 text-sm" /><button className="rounded-lg bg-gray-900 px-4 py-2.5 text-sm font-medium text-white">Link gönder</button>{message && <p className="text-sm text-gray-600">{message}</p>}</form></div></Layout>; }
