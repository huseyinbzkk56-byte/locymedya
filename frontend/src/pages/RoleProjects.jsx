import { useEffect, useState } from 'react';
import Layout from '../components/Layout';
import { apiFetch } from '../api/client';
export default function RoleProjects({ title = 'Projelerim' }) { const [projects, setProjects] = useState([]); const [videos, setVideos] = useState([]); const [error, setError] = useState(''); useEffect(() => { Promise.all([apiFetch('/projects'), apiFetch('/videos')]).then(([projectData, videoData]) => { setProjects(projectData.projects); setVideos(videoData.videos); }).catch((err) => setError(err.message)); }, []); return <Layout title={title}><div className="max-w-5xl mx-auto"><h1 className="text-3xl font-semibold">{title}</h1>{error && <p className="mt-4 text-sm text-red-600">{error}</p>}<div className="mt-6 space-y-3">{projects.map((project) => <article key={project.id} className="rounded-xl border border-gray-200 p-4"><div className="flex flex-col gap-2 sm:flex-row sm:items-center sm:justify-between"><div><h2 className="font-medium">{project.name}</h2><p className="text-sm text-gray-500">{project.artist_name || 'Sanatçı'} · {project.start_date || 'Tarih yok'}</p></div><span className="rounded-full bg-gray-100 px-2.5 py-1 text-xs text-gray-600">{project.status}</span></div><div className="mt-3 text-sm text-gray-500">{videos.filter((video) => video.project_id === project.id).length} video kaydı</div></article>)}{!projects.length && <p className="text-sm text-gray-400">Henüz atanmış proje yok.</p>}</div></div></Layout>; }
